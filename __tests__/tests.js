const { getLargestTrappedWater, findBiggestWaterTrap } = require('../server/utils.js');

test('does jest work?', () => {
	expect(1 + 1).toBe(2);
});


test('does jest work?', () => {
	expect(1 + 1).toBe(2);
});